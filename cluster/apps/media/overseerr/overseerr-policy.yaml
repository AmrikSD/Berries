---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: overseerr
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: overseerr
  policy:
    semver:
      range: 1.x.x
